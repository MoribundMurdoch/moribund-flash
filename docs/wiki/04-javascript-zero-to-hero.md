# JavaScript Zero to Hero

This project uses plain JavaScript modules.

A module is a file that can export functions and import functions from other files.

Example:

```js
import { listDecks } from "./api.js";
