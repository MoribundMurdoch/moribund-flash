# `src/screens/deckList.js`

Purpose:
- Lists saved decks from the backend.
- Routes selected decks into Study Mode.

Known goblins:
- Requires Rust `list_decks`.
- Deck click route must match `main.js`.
