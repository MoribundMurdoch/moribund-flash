# `src/styles.css`

Purpose:
- Global styles.
- Imports screen-specific CSS files.
- Contains shared app styling.

Known goblins:
- Duplicate screen rules can override screen-specific CSS.
- `@import` order matters.
