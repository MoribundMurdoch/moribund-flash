# Media Not Showing

Checklist:
- Does the `.mflash` card have `media`?
- Does `media.path` point to a real file?
- Was the file copied into app data?
- Is asset protocol enabled?
- Is CSP blocking asset URLs?
- Is Study Mode converting local paths correctly?
