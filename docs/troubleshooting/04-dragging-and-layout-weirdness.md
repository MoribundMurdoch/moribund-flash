# Dragging and Layout Weirdness

Checklist:
- Is the draggable thing in normal document flow?
- Is it `position: absolute` or still margin-centered?
- Is the drag canvas fixed to the viewport?
- Are duplicate CSS rules overriding the intended layout?
