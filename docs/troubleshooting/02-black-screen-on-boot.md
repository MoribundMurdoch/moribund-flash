# Black Screen on Boot

Checklist:
- Open DevTools console.
- Check for broken imports.
- Check `node --check` for edited JS files.
- Check CSP changes in `tauri.conf.json`.
- Check whether `main.js` imports a broken screen.
